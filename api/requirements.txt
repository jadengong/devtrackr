# Minimal requirements for Vercel deployment
# Only include essential dependencies

fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
python-dotenv==1.0.0

# Database dependencies (only if using database)
# sqlalchemy==2.0.43
# psycopg2-binary==2.9.10
# alembic==1.13.1

# Authentication dependencies (only if using auth)
# python-jose[cryptography]==3.3.0
# passlib==1.7.4
# bcrypt==4.1.2
